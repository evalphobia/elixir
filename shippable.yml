language: none

env:
  MIX_ENV: "test"
  PATH: "${PATH}:${HOME}/erlang/bin"

build:
  cache_dir_list:
    - "${HOME}/cache"
  pre_ci:
    - source /dev/stdin <<< "$(cat install_erlang.sh)"
  ci:
    - "make compile && rm -rf .git && make test"
