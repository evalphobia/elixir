box: elixir:1.2

build:
  steps:
    - script:
        name: set env vars
        code: |
          export MIX_ENV=test

    - script:
        name: test
        code: |
          cd $WERCKER_SOURCE_DIR
          make compile && rm -rf .git && make test
